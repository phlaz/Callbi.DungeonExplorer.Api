# Callbi Dungeon Explorer

Clone the repo.

From the solution root run 
	docker-compose up --build 

The frontend is deployed to http://localhost:3000
If you want to change this, you'll need to change the API config setting "FrontendUrl" in appsetting.json as well as the client Dockerfile.